//import axios from 'axios';

const state = {
    userid: 1,
}
const getters = {
    userid: (state) => state.userid 
}
const actions = {}
const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}